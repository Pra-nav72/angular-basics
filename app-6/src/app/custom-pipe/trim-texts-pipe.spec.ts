import { TrimTextsPipe } from './trim-texts-pipe';

describe('TrimTextsPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimTextsPipe();
    expect(pipe).toBeTruthy();
  });
});
